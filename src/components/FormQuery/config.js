// 基础默认配置
// form 配置
export const form = {
  labelAlign: 'left',
  layout: 'inline',
  scrollToFirstError: true
};

// formItem 配置
export const formItem = {
  trigger: 'onChange'
};

// row 配置
export const row = {
  align: 'top',
  gutter: [24],
  justify: 'start',
  wrap: true
};

// col 配置
export const col = {
  span: 6
};
